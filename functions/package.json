{
  "name": "ohsms-functions",
  "description": "Notification tier for WEHS/OHSMS — Firestore triggers and scheduled digests.",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "logs": "firebase functions:log",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
