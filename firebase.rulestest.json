{
  "_comment": "Rules-test-only emulator config. Same rules file, different ports, so `npm run test:rules` never collides with — or clears — a Firestore emulator already serving local development on 8080.",
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "emulators": {
    "firestore": {
      "port": 8085
    },
    "storage": {
      "port": 9198
    },
    "ui": {
      "enabled": false
    },
    "singleProjectMode": true
  }
}
