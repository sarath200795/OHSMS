// Straight from the shared kit. Audit's call sites spell the body copy
// `message`, which the shared EmptyState accepts alongside `description`/`hint`.
export { EmptyState as default } from '../../../../shared/ui'
