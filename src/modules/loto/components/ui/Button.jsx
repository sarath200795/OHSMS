// Straight from the shared kit. The shared Button carries the `size` scale
// (sm/md/lg) and maps this module's `steel` variant onto the design system's
// `soft`, so every loto call site keeps working unchanged.
export { Button as default } from '../../../../shared/ui'
